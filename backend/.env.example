# 数据库配置（Railway 会自动提供 DATABASE_URL）
# DATABASE_URL=postgresql://user:password@host:port/dbname

# JWT 密钥（生产环境必须修改为随机字符串）
SECRET_KEY=change-this-to-a-random-secret-key-in-production

# CORS 允许的前端域名（多个域名用逗号分隔或重复设置）
# Railway 环境变量格式：BACKEND_CORS_ORIGINS=["https://your-domain.vercel.app"]
BACKEND_CORS_ORIGINS=["http://localhost:5173","http://localhost:5177","https://eco-learn.vercel.app"]
